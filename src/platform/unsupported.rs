pub struct PlatformApp;

impl PlatformApp {
    pub fn new() -> Self {
        Self
    }

    pub fn run(&mut self) {
        eprintln!("popflare is currently planned as a macOS-only app.");
    }
}
